## Parent PRD
`.seed/003/prd.md`

## What to build
Add a `capitalize` function to `string_utils.py` that capitalizes the first letter of a string. Use TDD - write tests first, then implementation.

## Acceptance criteria
- [ ] Create `string_utils.py` with `capitalize` function [AFK]
- [ ] Write tests for `capitalize` function [AFK]
- [ ] Tests pass [AFK]
- [ ] Handle edge cases: empty string, single char, already capitalized [AFK]

## Blocked by
None - can start immediately

## User stories addressed
- US-001
